platform :ios, '8.0'
use_frameworks!

target 'Drift' do
    pod 'ObjectMapper', '~> 1.3'
    pod 'LayerKit', '~> 0.17'
end

target 'DriftTests' do
    pod 'ObjectMapper', '~> 1.3'
    pod 'LayerKit', '~> 0.17'
end
